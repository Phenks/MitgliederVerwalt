<UserControl x:Class="MitgliederVerwaltung.Views.TransaktionView.TranskationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MitgliederVerwaltung.Views.TransaktionView"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.ColumnSpan="4" Grid.Column="0" Background="NavajoWhite"></Border>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Sender}"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Verwendungszweck}"></TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Betrag}"></TextBlock>
    </Grid>
</UserControl>
